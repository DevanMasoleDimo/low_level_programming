#include <stdio.h>
/**
 * main - Prog Entry point
 * Return: Always 0 (Success0
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
